export enum SpeedEnum {
  Slow = 'slow',
  Moderate = 'moderate',
  Fast = 'fast',
}

export enum SizeEnum {
  XL = 'xl',
  L = 'l',
  M = 'm',
  S = 's',
}

export enum DIRECTION {
  IDLE = 0,
  UP = 1,
  DOWN = 2,
  LEFT = 3,
  RIGHT = 4,
}
